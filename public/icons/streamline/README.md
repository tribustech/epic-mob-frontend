# Wizard icons — Streamline Colors

These are **placeholder** SVGs (warm tile + emoji) so the wizard renders today.
Swap each one 1:1 with the real download from
https://www.streamlinehq.com/icons/streamline-colors — **keep the same filename**
and the wizard picks it up automatically (referenced from `lib/quote-wizard.ts`).
